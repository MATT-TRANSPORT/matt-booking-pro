import { redirect } from "next/navigation";import { createClient } from "@/lib/supabase/server";import PanelNav from "@/components/PanelNav";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const {data}=await s.from("bookings").select("*").order("travel_date").order("travel_time");return <main className="container"><h1>Rezerwacje</h1><PanelNav/><div className="card"><table className="table"><thead><tr><th>Numer</th><th>Termin</th><th>Klient</th><th>Cena</th><th>Status</th></tr></thead><tbody>{(data??[]).map((b:any)=><tr key={b.id}><td>{b.booking_number}</td><td>{b.travel_date}<br/>{b.travel_time}</td><td>{b.customer_name}<br/>{b.phone}</td><td>{Number(b.total_price).toFixed(2)} zł</td><td><span className={`status ${b.status}`}>{b.status}</span></td></tr>)}</tbody></table></div></main>}
