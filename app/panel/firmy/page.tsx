import PanelNav from "@/components/PanelNav";import {panelClient} from "@/lib/panel";
export default async function Page(){const {s}=await panelClient();const {data:companies}=await s.from("companies").select("*").order("name");const ids=(companies??[]).map((x:any)=>x.id);const {data:bookings}=ids.length?await s.from("bookings").select("company_id,total_price,status").in("company_id",ids):{data:[] as any[]};return <main className="container"><h1>Firmy B2B</h1><PanelNav/><div className="company-admin-grid">{(companies??[]).map((c:any)=>{const rows=(bookings??[]).filter((b:any)=>b.company_id===c.id);const value=rows.reduce((a:number,b:any)=>a+Number(b.total_price||0),0);const active=rows.filter((b:any)=>["pending","confirmed","assigned","in_progress","picked_up"].includes(b.status)).length;return <a href={`/panel/firmy/${c.id}`} className="card company-admin-card" key={c.id}><span className="badge">{c.active?"AKTYWNA":"NIEAKTYWNA"}</span><h2>{c.name}</h2><p className="muted">NIP: {c.nip||"—"}</p><div className="company-mini-stats"><div><strong>{active}</strong><span>Aktywne</span></div><div><strong>{rows.length}</strong><span>Wszystkie</span></div><div><strong>{value.toFixed(0)} zł</strong><span>Wartość</span></div></div></a>})}</div></main>}
