import { redirect } from "next/navigation";import { createClient } from "@/lib/supabase/server";import PanelNav from "@/components/PanelNav";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const {data}=await s.from("drivers").select("*").order("full_name");return <main className="container"><h1>Kierowcy</h1><PanelNav/><div className="grid">{(data??[]).map((d:any)=><div className="card" key={d.id}><h2>{d.full_name}</h2><p>{d.phone}</p><p className="muted">Status: {d.status}</p></div>)}</div></main>}
