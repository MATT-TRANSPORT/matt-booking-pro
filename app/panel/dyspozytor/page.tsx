import { redirect } from "next/navigation";import { createClient } from "@/lib/supabase/server";import PanelNav from "@/components/PanelNav";import DispatcherClient from "@/components/DispatcherClient";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const today=new Date().toISOString().slice(0,10);const [{data:bookings},{data:drivers},{data:vehicles}]=await Promise.all([s.from("bookings").select("*").eq("travel_date",today).order("travel_time"),s.from("drivers").select("*").order("full_name"),s.from("vehicles").select("*").order("name")]);return <main className="container"><h1>Dyspozytor</h1><PanelNav/><DispatcherClient bookings={bookings??[]} drivers={drivers??[]} vehicles={vehicles??[]}/></main>}
