"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";export default function Page(){const [password,setPassword]=useState(""),[repeat,setRepeat]=useState(""),[msg,setMsg]=useState(""),[saving,setSaving]=useState(false);async function save(){if(password.length<8){setMsg("Hasło musi mieć co najmniej 8 znaków.");return}if(password!==repeat){setMsg("Hasła nie są takie same.");return}setSaving(true);const s=createClient();const {error}=await s.auth.updateUser({password});setSaving(false);setMsg(error?error.message:"✓ Hasło ustawione. Możesz się zalogować.")}return <main className="container password-setup-shell"><div className="card"><span className="badge">MATT TRANSPORT</span><h1>Ustaw hasło</h1><label>Nowe hasło<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Powtórz hasło<input type="password" value={repeat} onChange={e=>setRepeat(e.target.value)}/></label><button className="btn" disabled={saving} onClick={save}>{saving?"ZAPISYWANIE...":"USTAW HASŁO"}</button>{msg&&<div className="admin-save-message">{msg}</div>}<div className="access-login-links"><a href="/kierowca/login">Panel kierowcy</a><a href="/firma/login">Panel firmy</a></div></div></main>}
