"use client";import {useState} from "react";export default function PortalAccessButton({type,id,active}:{type:"driver"|"company",id:string,active?:boolean}){const [msg,setMsg]=useState(""),[loading,setLoading]=useState(false);async function run(){setLoading(true);setMsg("");const rr=await fetch("/api/admin/portal-access",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,id})});const d=await rr.json();setLoading(false);setMsg(rr.ok?"✓ Link do ustawienia hasła wysłany.":(d.error??"Błąd."))}return <div className="portal-access-box"><button className="btn secondary" disabled={loading} onClick={run}>{loading?"WYSYŁANIE...":active?"WYŚLIJ LINK / RESET HASŁA":"UTWÓRZ KONTO"}</button>{msg&&<small>{msg}</small>}</div>}
